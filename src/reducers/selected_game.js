const initState = {
  game: {},
  loading: true,
};

const selectedGameReducer = (state=initState, action) => {
  switch (action.type) {
    case
    default:
      return state
  }
};

export default selectedGameReducer;
